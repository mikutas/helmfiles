repositories:
  - name: fairwinds-stable
    url: https://charts.fairwinds.com/stable

releases:
  - chart: fairwinds-stable/polaris
    name: polaris
    namespace: polaris
    version: 6.0.1
    values:
      - dashboard:
          ingress:
            enabled: true
            hosts:
              - polaris.local
