repositories:
  - name: jetstack
    url: https://charts.jetstack.io

releases:
  - chart: jetstack/trust-manager
    name: trust-manager
    namespace: cert-manager
    version: v0.25.0
    values:
      - values.yaml
