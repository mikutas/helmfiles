repositories:
  - name: odigos
    url: https://odigos-io.github.io/odigos

releases:
  - name: odigos
    namespace: odigos-system
    chart: odigos/odigos
    version: 1.35.3

  - name: ingress
    namespace: odigos-system
    chart: ../../../charts/ingresses
    values:
      - ingress.values.yaml
